- hosts: localhost # use all for remote
  vars: 
    date: "2020-08-09"
  tasks:
    - name: copy release note
      template:
        src: ../vault/dendron.template.release.md
        dest: "../vault/dendron.release.{{ date }}.md"